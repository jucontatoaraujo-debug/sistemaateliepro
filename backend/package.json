{
  "name": "atelier-frontend-deploy",
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix ../frontend install && npm --prefix ../frontend run build",
    "start": "node frontend-server.js"
  }
}
